1. to run the code just run the shell script.
  a. output is formatted as follows:
    F1 score
    precision score
    recall

2. this will run the experiment with the svm and logistic regression.

3. if you want to run cross validation uncomment experiment_svm() and experiment_logistic_regression() both functions at the bottom of the file in src/assign.py

